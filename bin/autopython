#!/usr/bin/env python

from autopython.autopython import Presenter
from autopython import console
import sys

presenter = Presenter(sys.argv[1], width=console.getwidth(),
                      colors=True, animation=True, logging=True)
presenter.run()
